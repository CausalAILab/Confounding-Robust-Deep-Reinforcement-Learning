train:
  id: null
  done_on_life_loss: True
  size: 64
  max_episode_steps: null
  num_stack: 4
  
test:
  id: ${..train.id}
  done_on_life_loss: False
  size: ${..train.size}
  max_episode_steps: null
  num_stack: 4

keymap: atari/${.train.id}